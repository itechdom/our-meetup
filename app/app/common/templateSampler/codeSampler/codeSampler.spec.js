//this component will take an html (bound to scope) and highlight its syntax
//it uses highlightSyntax directive
//maybe some E2E to make sure everything is styled correctly
var assert = require("assert");
describe('codeSampler Component', function() {
	it('should display the html view from main.html correctly', function () {

	});
});
