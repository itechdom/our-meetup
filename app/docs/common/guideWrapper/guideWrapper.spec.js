//this component only transcludes, so my test would be more integration if it was done
//we can test if the transclusion is done and scopes are bound fine
var assert = require("assert");
describe('guideWrapper Component', function() {
	it('should display the html view from main.html correctly', function () {

	});
});
