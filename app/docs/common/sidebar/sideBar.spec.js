//this component only iterates through the guidelinks and displays them
//maybe some E2E to make sure everything is styled correctly
var assert = require("assert");
describe('sidebar Component', function() {
	it('should display the html view from main.html correctly', function () {

	});
});
